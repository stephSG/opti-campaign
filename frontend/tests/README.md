# Frontend Tests

This directory contains the unit and component tests for the frontend application.

## Running Tests

To run the tests, navigate to the `frontend` directory and use the following command:

```bash
npm test
```

This will execute all `*.spec.js` files within the `frontend` directory using Vitest.
